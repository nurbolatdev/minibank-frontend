export default function AppSettings() {
    return (
        <div className="card">
            <h2 className="card-title">App Settings</h2>
            <p className="muted">
                Here you can later add theme, language, notifications, and app preferences.
            </p>
        </div>
    );
}